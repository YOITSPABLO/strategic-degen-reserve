const express = require('express'); const cors = require('cors'); const app = express(); app.use(express.json()); app.use(cors({ origin: 'https://strategicdegenreserve.netlify.app' })); app.get('/status', (req, res) => res.json({ message: 'Backend running', time: new Date().toISOString() })); app.post('/distribute', (req, res) => res.json({ success: true, distributed: 0.1, signature: 'test_signature' })); const PORT = process.env.PORT || 3000; app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
